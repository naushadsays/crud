1. Spring Webflux Employee CRUD API
2. Redis integration, custom TTL of 10min specified in config file. Search in Redis if found in redis then return the data if not then 
3. R2dbc db storage and return from Postgres DB.
4. Global error handling @RestConntrollerAdvice + @ExceptionHandler
5. Logging with @SLF4J Lombok
6. Performance testing using Jmeter
